exports.seed = function(knex) {
  console.log('Knex migrations has set up schema for products and images tables.  Run npm run dataImg and npm run dataProd to generate CSV, then npm run seed to seed postgres database')
};
